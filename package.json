{
  "name": "jutils",
  "version": "1.0.0",
  "description": "",
  "main": "src/index.ts",
  "scripts": {
    "build": "npx tsc && npx tsc-alias",
    "start": "pnpm run build && node dist",
    "watch": "npx tsc -w && npx tsc-alias -w",
    "deploy": "pnpm run clean && pnpm run build && node dist/deploy-commands.js",
    "clean": "npx rimraf dist",
    "prepare": "simple-git-hooks"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@types/node": "^24.9.1",
    "prisma": "^6.18.0",
    "simple-git-hooks": "^2.13.1",
    "tsx": "^4.20.6",
    "typescript": "^5.8.3"
  },
  "dependencies": {
    "@prisma/client": "^6.18.0",
    "discord.js": "^14.19.2",
    "dotenv": "^16.5.0"
    ,"date-fns": "^2.30.0",
    "date-fns-tz": "^1.3.7"
  },
  "pnpm": {
    "onlyBuiltDependencies": [
      "@prisma/client"
    ]
  }
}
